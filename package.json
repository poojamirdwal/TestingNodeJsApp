{
  "name": "click-counter-app",
  "version": "1.0.0",
  "description": "Simple Node.js app that collects clicks from users and displays the count",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
